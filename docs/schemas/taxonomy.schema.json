{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Taxonomy",
  "type": "object",
  "required": ["arcs"],
  "additionalProperties": false,
  "properties": {
    "arcs": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "layer", "categories"],
        "additionalProperties": false,
        "properties": {
          "name": { "type": "string", "minLength": 1 },
          "layer": {
            "description": "Columbia openness ontology layer (GPAIS tech stack) this arc represents.",
            "enum": ["product_ux", "model_components", "infrastructure"]
          },
          "categories": {
            "type": "array",
            "items": { "type": "string", "pattern": "^[a-z0-9_]+$" }
          }
        }
      }
    }
  }
}
