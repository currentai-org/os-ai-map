[project]
name = "os-ai-map"
version = "0.1.0"
description = "Open Source AI Map — curated data + modeling behind the AI Stack Map"
requires-python = ">=3.12"
dependencies = ["pyyaml>=6.0", "pyoso", "marimo", "pandas", "plotly", "jsonschema>=4.0"]

[dependency-groups]
dev = ["pytest>=8.0"]
