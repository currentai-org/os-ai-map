name: ouroboros
display_name: Ouroboros
type: software
description: 'Agent OS: the agent gets smarter on its own. We just hold the line: the grading command and expected
  result never make it into the success contract we hand it. Interview-gated, staged evaluation, budgeted evolution
  loop. MCP server, 13 runtimes: Claude Code, Codex CLI, Gemini CLI, OpenCode, Copilot, Kiro and more. Skip the
  prompt engineering. The agent runs, fails, and gets smarter every generation. The grading command and expected
  result never make it into the success contract we hand it.'
github:
- url: https://github.com/Q00/ouroboros
comments: Verified 2026-08-30 via the GitHub API, the LICENSE body and the repository README.
