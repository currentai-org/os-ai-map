name: textract
display_name: Amazon Textract
type: software
description: Managed document-analysis service from AWS that extracts text, handwriting, tables, forms and
  key-value pairs from scanned documents, with query-based extraction that answers a natural-language question
  against a document rather than returning the whole page.
comments: Verified 2026-09-16 via the AWS product page.
