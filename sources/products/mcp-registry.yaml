name: mcp-registry
display_name: Official MCP Registry
type: software
description: The Model Context Protocol's own catalogue of MCP servers - an API and a hosted service that lists
  what servers exist and how to reach them, with a publishing flow for server authors. Run by the protocol's
  maintainers rather than a vendor.
github:
- url: https://github.com/modelcontextprotocol/registry
comments: GitHub's classifier reports NOASSERTION because the LICENSE opens with a licensing transition notice
  before the Apache text; the body is the same file the specification repository carries.
