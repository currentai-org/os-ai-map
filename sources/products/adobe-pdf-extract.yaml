name: adobe-pdf-extract
display_name: Adobe PDF Extract API
type: software
description: Document-extraction API from Adobe that returns a PDF's text, tables and figures as structured
  JSON with the reading order and styling information the format carries, built on Adobe's own PDF engine rather
  than on OCR of a rendered page.
