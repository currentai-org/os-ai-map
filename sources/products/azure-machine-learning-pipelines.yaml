name: azure-machine-learning-pipelines
display_name: Azure Machine Learning Pipelines
type: software
description: Managed pipeline service in Azure Machine Learning. A pipeline breaks a machine-learning task
  into components that Azure runs in sequence on compute targets the workspace defines, managing the dependencies
  between steps and reusing cached outputs where inputs have not changed.
comments: Managed Azure service with no self-hostable implementation.
