name: mcp-csharp-sdk
display_name: MCP C# SDK
type: software
description: The official C# SDK for the Model Context Protocol, maintained in collaboration with Microsoft.
  Client and server, all three primitives, and stdio, Streamable HTTP and legacy SSE transports.
github:
- url: https://github.com/modelcontextprotocol/csharp-sdk
comments: NuGet publishes a cumulative download total rather than a windowed one, so the 31,852,326 figure
  on `ModelContextProtocol` is all-time and cannot be banded on a monthly scale; the adoption record bands
  on stars instead and records why.
