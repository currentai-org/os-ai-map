name: reducto
display_name: Reducto Parse
type: software
description: Document-ingestion API aimed at retrieval pipelines. It parses PDFs, images, spreadsheets and
  Office documents into layout-aware JSON and into chunks sized and bounded for embedding, so the output is
  shaped for a retrieval index rather than for a human reader.
comments: Verified 2026-09-16 via the Reducto homepage.
