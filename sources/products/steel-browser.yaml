name: steel-browser
display_name: Steel Browser
type: software
description: 'Open-source browser API for agents: a batteries-included sandbox that manages sessions, pages
  and browser processes behind an HTTP API, self-hostable with Docker or run as Steel Cloud.'
github:
- url: https://github.com/steel-dev/steel-browser
comments: The npm `steel-sdk` package is a client for the API rather than the product, so adoption bands on
  stars instead.
