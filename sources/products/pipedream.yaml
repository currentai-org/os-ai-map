name: pipedream
display_name: Pipedream Connect
type: software
description: 'Managed authentication and tool-calling for agents: Pipedream runs the OAuth flow, stores and
  refreshes the tokens, and injects them per user on every request across roughly 3,000 APIs and 10,000 prebuilt
  tools.'
comments: 'Scored as Connect, the managed-auth product, rather than Pipedream''s workflow platform. Note for
  a later re-read: the integration counts on the documentation pages are rendered client-side and absent from
  the fetched bytes - the figures here come from the product page, which carries them. Verified 2026-09-17
  via the product page and the Connect documentation.'
