name: xllm
display_name: xLLM
type: software
description: xLLM is an efficient LLM inference framework specifically optimized for Chinese AI accelerators. It
  separates a service layer that handles scheduling and availability from an engine layer that handles computation,
  and is deployed at scale across JD.com's core retail business.
github:
- url: https://github.com/xLLM-AI/xllm
comments: The `xllm` package on PyPI is an unrelated project, so no package artifact is declared. Verified 2026-08-31
  via GitHub, the LICENSE body and the repository README.
