name: vearch
display_name: Vearch
type: software
description: Vearch is a distributed vector database for similarity search over embeddings, combining vector retrieval
  with scalar filtering in hybrid queries. It is written in Go and C++ and deploys as a cluster with separate master,
  router and partition-server roles.
github:
- url: https://github.com/vearch/vearch
