# Swept 2026-09-07 (weekly tail batch), same Hugging Face pass as base_pretrained; see that file's
# header for the query set and the predeclared retrieval cutoff.
#
# Only two rows. Nearly every instruction-tuned candidate the ranked queries returned is a release
# or a third-party redistribution of a family the map already carries - Qwen, GLM, DeepSeek, Gemma,
# Llama, Kimi, MiniMax, Nemotron, SmolLM, MiniCPM, Apertus, Ornith - and folds to a duplicate under
# the release-suffix rule rather than a candidate. The abliterated, uncensored and GGUF re-uploads
# that rank alongside them are parked as third-party derivatives.
#
# Counts, parked candidates with reasons, source URLs and fetch dates: docs/sweeps/2026-09-07-tail-batch.md.
category: finetuned_chat
products:
  - slug: dolphin
    display_name: Dolphin
    type: model
    org: dphn
    huggingface_model: dphn/dolphin-2.9.1-yi-1.5-34b
  - slug: exaone
    display_name: EXAONE
    type: model
    org: lg-ai-research
    huggingface_model: LGAI-EXAONE/EXAONE-3.5-7.8B-Instruct
